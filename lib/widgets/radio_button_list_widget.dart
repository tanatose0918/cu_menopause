import 'package:flutter/material.dart';
    
class RadioButtonListWidget extends StatefulWidget {
  const RadioButtonListWidget({Key? key}) : super(key: key);

  @override
  _RadioButtonListWidgetState createState() => _RadioButtonListWidgetState();
}

class _RadioButtonListWidgetState extends State<RadioButtonListWidget> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}